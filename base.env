# Only for Development
# Enable debugging, change the UV path to a directory that we can write
# to (because /venv is owned by root), and enable installing devlopment
# dependencies like pytest.
DEBUG=True
UV_PROJECT_ENVIRONMENT=/home/nonroot/venv/
UV_TOOL_BIN_DIR=/home/nonroot/venv/bin/
UV_NO_DEV=0

# Must be changed for prod
POSTGRES_PASSWORD=numbat
SECRET_KEY="django-insecure-e\$efkmcw8vv&9!pzyew1^y(+ir*gl0uw_fa3_1sj5=mo_ps&sf"
ALLOWED_HOSTS=*

# Reasonable defaults
POSTGRES_DB=numbat_tasks
POSTGRES_USER=numbat
