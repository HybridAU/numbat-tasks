import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    h1: {
      fontSize: "1.5rem",
    },
    button: {
      textTransform: "none",
    },
  },
});

export default theme;
